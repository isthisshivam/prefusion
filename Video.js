import React, {useState, useEffect, useRef} from 'react';
import {
  SafeAreaView,
  StatusBar,
  useColorScheme,
  Text,
  PermissionsAndroid,
  Alert,
  AppState,
} from 'react-native';
const Video = props => {
  return <></>;
};
export default Video;
